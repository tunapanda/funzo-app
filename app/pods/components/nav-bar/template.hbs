<nav class="navbar">
  <div class="container-fluid">
    {{#if nav.showBackButton}}
      <ul class="nav navbar-nav navbar-left">
        <li><a href="#" {{action 'back'}}><i class="material-icons">keyboard_arrow_left</i></a></li>
      </ul>
    {{/if}}

    <a class="navbar-brand">{{nav.title}}</a>

    <ul class="nav navbar-nav navbar-right">
      {{#if session.isAuthenticated}}
        <li class="dropdown">
          <a href="#" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><i class="material-icons">account_circle</i></a>
          <ul class="dropdown-menu">
            <li>{{#link-to 'profile'}}profile{{/link-to}}</li>
            <li class="divider"></li>
            <li><a href="#!" {{action 'logout'}}>logout</a></li>
          </ul>
        </li>
      {{/if}}
      <li>{{#link-to 'settings'}}<i class="material-icons">settings</i>{{/link-to}}</li>
    </ul>
  </div>
  </nav>