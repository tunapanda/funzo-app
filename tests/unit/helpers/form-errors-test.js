import { formErrors } from '../../../helpers/form-errors';
import { module, test } from 'qunit';

module('Unit | Helper | form errors');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formErrors(42);
  assert.ok(result);
});
